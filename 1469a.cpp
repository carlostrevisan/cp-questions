#include <bits/stdc++.h>
using namespace std;
int main (void) {
    int n;
    string s;
    cin >> n;
    for (int i = 0; i < n; i++) {
        cin >> s;
        for (int i = 0; i < s.size(); i++) {
        }
        cout << s << endl;
    }
    return 0;
}
